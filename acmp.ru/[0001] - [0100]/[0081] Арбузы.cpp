#include <iostream>
using namespace std;

int main() {
    int n, min, max;
    cin >> n >> min;
    max = 0;

    for (int i = 0; i < n; i++) {
        cin >> n;
    }
    
    cout << min(arr) << ' ' << max(arr) << endl;
}